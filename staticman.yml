# Name of the property. You can have multiple properties with completely
# different config blocks for different sections of your site.
# For example, you can have one property to handle comment submission and
# another one to handle posts.
comments:
  # (*) REQUIRED
  #
  # Names of the fields the form is allowed to submit. If a field that is
  # not here is part of the request, an error will be thrown.
  allowedFields: ["name", "email", "url", "message", "replying_to"]

  # (*) REQUIRED WHEN USING NOTIFICATIONS
  #
  # When allowedOrigins is defined, only requests sent from one of the domains
  # listed will be accepted. The origin is sent as part of the `options` object
  # (e.g. <input name="options[origin]" value="http://yourdomain.com/post1")
  allowedOrigins: ["blog.claytonsanford.com"]

  # (*) REQUIRED
  #
  # Name of the branch being used. Must match the one sent in the URL of the
  # request.
  branch: "gh-pages"

  # (*) REQUIRED
  #
  # Destination path (filename) for the data files. Accepts placeholders.
  filename: "comment-{@timestamp}"

  # The format of the generated data files. Accepted values are "json", "yaml"
  # or "frontmatter"
  format: "yaml"

  # List of fields to be populated automatically by Staticman and included in
  # the data file. Keys are the name of the field. The value can be an object
  # with a `type` property, which configures the generated field, or any value
  # to be used directly (e.g. a string, number or array)
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
#        format: "timestamp-seconds"

  # Whether entries need to be appproved before they are published to the main
  # branch. If set to `true`, a pull request will be created for your approval.
  # Otherwise, entries will be published to the main branch automatically.
  moderation: false

  # Name of the site. Used in notification emails.
  #name: "eduardoboucas.com"
  name: "blog.claytonsanford.com"

  # Notification settings. When enabled, users can choose to receive notifications
  # via email when someone adds a reply or a new comment. This requires an account
  # with Mailgun, which you can get for free at http://mailgun.com.
  notifications:
    # Enable notifications
    enabled: true

    # (!) ENCRYPTED
    # Mailgun API key
    apiKey: "Yuk0kK1+2Wq/JXUHMR5EtNK9YTKX+VgsDtRuP6srNhpwg+I5i6z6T3/Flv+L32/tZWl/DHcDBz9a2/wuCXScbMA5+F0YVcKzDas+pVu9mkhK+wBWIxwqs2rTwG322FAimwqFKrNAbEzm2476GECVfwq+p5VVlaVfmIdopAt4c4k="

    # (!) ENCRYPTED
    # Mailgun domain (encrypted)
    domain: "E5Z+naUBWIDusrP5T9k3n1aiNrcanf1Vkvm3U1eQlvv8Jv7q4uw4+mzjtPNqnf5ZFcT301EwPUdodOm++r5qtXxOQCVD38vcU/rKQE6Pr124S1nEXN1kKTWID54qjV91KyVTuIKJxGnkYJBkUsOr4UlTG3V05Ksj8/DUJ0n6OpE="
    
  # (*) REQUIRED
  #
  # Destination path (directory) for the data files. Accepts placeholders.
  path: "_data/comments/{options.slug}"

  # Names of required files. If any of these isn't in the request or is empty,
  # an error will be thrown.
  requiredFields: ["name", "message"]

  # List of transformations to apply to any of the fields supplied. Keys are
  # the name of the field and values are possible transformation types.
  transforms:
    email: md5

  reCaptcha:
    enabled: true
    siteKey: "6LeSD4ccAAAAADR7VUKl6MQPx4Q-jKFO95GnmoJq"
    secret: "RXpHwKuXJ8kpp+1IJtLXq15vJtLTiwsQSPiKyaxXUADLTLXqhTTa6Q7CtlX9Yqku+YFK5wMFb1c4vd5R3dtEc5FfWLJ37kTwKnhOKwFheld2l6jjknO5mUrh3ugdwmDoCt15jRLNv5BnuHIf5D7hIJ0GdT3qvLpun8kjxZlDepA="

  # Akismet spam detection
  akismet:
    enabled: true